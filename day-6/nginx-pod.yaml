apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
    volumeMounts:
    - name: meu-pvc-nfs
      mountPath: /usr/share/nginx/html
  volumes:
  - name: meu-pvc-nfs
    persistentVolumeClaim:
      claimName: meu-pvc-nfs        



